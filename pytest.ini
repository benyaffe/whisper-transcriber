[pytest]
# Only collect the real suite. tests/manual/ holds hand-run QA scripts whose
# functions are named test_* but return bools, open GUI windows, or drive a
# built .app -- pytest must not collect them.
testpaths = tests
norecursedirs = manual venv venv.broken build dist .git

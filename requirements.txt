# Core transcription
faster-whisper>=1.0.0

# Video downloads
yt-dlp>=2024.1.0

# GUI
PyQt6>=6.6.0

# App bundling
py2app>=0.28.0

# Audio processing (faster-whisper dependency, but explicit for clarity)
# ffmpeg must be installed separately via: brew install ffmpeg
